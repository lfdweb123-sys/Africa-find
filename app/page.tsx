export default function Home() {
  return (
    <main style={{ fontFamily: "sans-serif", padding: 40 }}>
      <h1>Africa Find</h1>
      <p>
        Moteur de recherche IA pour le commerce et les services numériques
        africains. Le service fonctionne via WhatsApp — le webhook est exposé
        sur <code>/api/webhook</code>.
      </p>
    </main>
  );
}
